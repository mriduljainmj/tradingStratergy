# Copy this file to .env and fill in your credentials
# .env is gitignored — never commit real secrets

# ── Trading mode ──────────────────────────────────────────────────────────────
# BACKTEST | PAPER | LIVE
APP_MODE=PAPER

# ── Kite Connect credentials ──────────────────────────────────────────────────
KITE_API_KEY=
KITE_API_SECRET=

# ── Dashboard server ──────────────────────────────────────────────────────────
DASHBOARD_HOST=127.0.0.1
DASHBOARD_PORT=8080

# ── Local dev user ────────────────────────────────────────────────────────────
# If set, this user is auto-created on first run (no manual registration needed).
# Leave blank on production / Render (real users register via the web form).
DEFAULT_USER_EMAIL=
DEFAULT_USER_PASSWORD=

# ── Auth / Security ───────────────────────────────────────────────────────────
# Used to sign JWT tokens. Local development generates .axiom_dev_secret.
# On Render: set this to a long random string in the Environment settings.
# JWT_SECRET_KEY=<at-least-32-random-characters>
# APP_ENV=production
# Keep a stable ENCRYPT_KEY when migrating existing encrypted broker credentials.
# ENCRYPT_KEY=<stable-secret>
# RESTORE_TRADING_SESSIONS=0
# Disable engine auto-start while retaining broker session restoration.
AUTO_START_TRADING=0

# ── Database ──────────────────────────────────────────────────────────────────
# Leave blank for local SQLite (auto-created as trading.db).
# On Render: set to your Neon/Supabase PostgreSQL connection string.
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
